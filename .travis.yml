language: objective-c
before_install:
  - brew update
  - brew uninstall xctool
  - brew install xctool
  - export LANG=en_US.UTF-8
  - gem i cocoapods --no-ri --no-rdoc
xcode_workspace: EDColor.xcworkspace
xcode_scheme: Tests
xcode_sdk: iphonesimulator
